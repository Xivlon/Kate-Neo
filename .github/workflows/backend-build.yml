name: Backend Build

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-build.yml'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-build.yml'

jobs:
  build:
    name: Build Backend
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Set up Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
    
    - name: Install dependencies
      working-directory: ./backend
      run: |
        echo "TODO: Install backend dependencies"
        echo "Skipping npm install in placeholder mode"
        # npm ci
    
    - name: Validate bridge.js syntax
      working-directory: ./backend
      run: |
        echo "Validating JavaScript syntax..."
        node --check bridge.js
        echo "✓ Syntax validation passed"
    
    # TODO: Add step to run backend tests
    # - name: Run tests
    #   working-directory: ./backend
    #   run: npm test
    
    # TODO: Add step to lint code
    # - name: Lint
    #   working-directory: ./backend
    #   run: npm run lint
    
    # TODO: Add step to check for security vulnerabilities
    # - name: Security audit
    #   working-directory: ./backend
    #   run: npm audit
    
    # TODO: Add step to build backend (when Kate integration is added)
    # - name: Build
    #   working-directory: ./backend
    #   run: npm run build
    
    - name: Upload backend artifacts
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: backend-build-${{ matrix.node-version }}
        path: |
          backend/bridge.js
          backend/package.json
        retention-days: 7

  # TODO: Add Kate engine integration test job
  # kate-integration:
  #   name: Test Kate Engine Integration
  #   needs: build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Set up Kate engine
  #       run: echo "TODO: Install Kate engine"
  #     - name: Test integration
  #       run: echo "TODO: Run integration tests"
